
export let errOptions = {
    color: "white",
    textColor:'red',
    position: "bottom",
    timeout:8000,
    classes:"notify-size"
}
export let sucOptions = {
  color: "white",
  textColor:'green',
  classes:"notify-size",
  position: "bottom",
  timeout:8000,
}
